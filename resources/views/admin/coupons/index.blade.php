@extends('layouts.admin') @section('content')<div class="crumb">PROMOTIONS / COUPONS</div><div class="topline"><h1>Mã giảm giá</h1><a class="btn lime" href="{{ route('admin.coupons.create') }}">+ TẠO MÃ</a></div><section class="panel"><table class="table"><thead><tr><th>MÃ</th><th>ƯU ĐÃI</th><th>ĐƠN TỐI THIỂU</th><th>ĐÃ DÙNG</th><th>HẾT HẠN</th><th></th></tr></thead><tbody>@forelse($coupons as $coupon)<tr><td><b>{{ $coupon->code }}</b></td><td>{{ $coupon->type==='percent'?$coupon->value.'%':number_format($coupon->value,0,',','.').'₫' }}</td><td>{{ number_format($coupon->minimum_order_value,0,',','.') }}₫</td><td>{{ $coupon->used_count }}{{ $coupon->usage_limit?' / '.$coupon->usage_limit:'' }}</td><td>{{ $coupon->expires_at?->format('d/m/Y') ?? 'Không giới hạn' }}</td><td><div class="actions"><a class="btn small" href="{{ route('admin.coupons.edit',$coupon) }}">SỬA</a><form method="POST" action="{{ route('admin.coupons.destroy',$coupon) }}">@csrf @method('DELETE')<button class="btn small danger">XÓA</button></form></div></td></tr>@empty<tr><td colspan="6">Chưa có mã giảm giá.</td></tr>@endforelse</tbody></table></section>@endsection
